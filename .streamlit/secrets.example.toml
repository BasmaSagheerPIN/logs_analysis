# Copy this file to .streamlit/secrets.toml for local development.
# On Render, add the same keys as environment variables or Streamlit secrets.

MONGO_URI = "mongodb://root:12345@localhost:27017/logbook?authSource=admin&appName=logbook"
MONGO_DB = "logbook"
MONGO_COLLECTION = "backend-logs"
DEFAULT_FETCH_LIMIT = 5000
